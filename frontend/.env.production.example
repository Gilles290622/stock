# If your API is on a different domain (shared hosting for static frontend only):
# VITE_API_BASE=https://api.jts-services.shop

# If frontend and backend share the same domain and path, leave it empty
# so axios uses relative paths like /api, /uploads (works with reverse proxy)
# VITE_API_BASE=
